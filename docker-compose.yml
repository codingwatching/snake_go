services:
  snake-game:
    build: .
    container_name: snake-game
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: always
    environment:
      - DETAILED_LOGS=true
      # Keep the container listening on 8080 so the port mapping / reverse proxy is unaffected.
      - PORT=8080
    env_file:
      - .env
    command: [ "./webserver", "--detailed-logs" ]
